const express = require('express');
const router = express.Router();
const config = require('../config/database');

const inventory = require('../models/inventory');

//TODO Functionalities with the inventory schema.


module.exports = router;
